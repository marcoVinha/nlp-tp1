# Installs uv, used to manage our Python project
curl -LsSf https://astral.sh/uv/install.sh | sh

# Installs unzip, to manipulate project data
apt install unzip

# Installs tree, used to visualize project structure
apt install tree
